streamlit>=1.36
pandas>=2.2
pyarrow>=15
plotly>=5.22
huggingface_hub>=0.25
streamlit-plotly-events>=0.0.6,<0.1.0

# add others you import:
# numpy, geopandas, shapely, altair, pydeck, etc.

# --- core app ---
streamlit==1.50.0
pandas==2.2.2
numpy==1.26.4

# parquet I/O (we use pyarrow engine)
pyarrow==16.1.0

# ISO3 country lookup
pycountry==22.3.5

# (optional) if you plan to generate PDFs with Matplotlib like our doc exporter
# matplotlib==3.9.2




